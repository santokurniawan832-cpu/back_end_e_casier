# 🧩 MyApp - Express + MySQL2 MVC

A simple Node.js project using **Express**, **MySQL2**, and the **MVC (Model-View-Controller)** pattern.

---

## 📁 Folder Structure
# npm install
# install sequalize orm
# install bcrypt
# install jwt
# install cors
# install migration sequalize sebagai orm
```
npm install --save-dev sequelize-cli
```
# install folder migration

```
npx sequelize-cli init
```